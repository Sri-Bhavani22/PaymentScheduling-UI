## 1. Base Font Size

- [ ] 1.1 Update `html` font-size from `16px` to `14px` in `src/index.css`

## 2. MUI Typography Scale

- [ ] 2.1 Add explicit `fontSize` to h4 variant (`1.5rem`) in `src/theme/theme.js`
- [ ] 2.2 Add explicit `fontSize` to h5 variant (`1.25rem`) in `src/theme/theme.js`
- [ ] 2.3 Add explicit `fontSize` to h6 variant (`1.1rem`) in `src/theme/theme.js`
- [ ] 2.4 Add body1 (`0.875rem`) and body2 (`0.8125rem`) typography overrides in `src/theme/theme.js`
- [ ] 2.5 Add subtitle1 (`0.875rem`) and subtitle2 (`0.8125rem`) typography overrides in `src/theme/theme.js`
- [ ] 2.6 Add caption (`0.7rem`) and button (`0.8125rem`) typography overrides in `src/theme/theme.js`

## 3. Verification

- [ ] 3.1 Run the application and visually verify all font sizes are reduced
- [ ] 3.2 Confirm heading hierarchy is preserved (h4 > h5 > h6 > body)
